# Este repositorio es PÚBLICO (lo requiere GitHub Pages gratuito).
# Nunca subas aquí estados de cuenta ni respaldos con tus datos financieros.

# Estados de cuenta y documentos del broker
*.pdf
estados/
estados-cuenta/
movimientos/
gbm/

# Datos exportados / respaldos del portafolio.
# Se ignora TODO .json salvo los de configuración del proyecto, para que ningún
# archivo con movimientos financieros termine publicado por accidente.
*.csv
*.tsv
*.xlsx
*.xls
*.json
!package.json
!package-lock.json
!manifest.webmanifest

# Basura del sistema y del editor
.DS_Store
Thumbs.db
node_modules/
.vscode/
.idea/
*.log
